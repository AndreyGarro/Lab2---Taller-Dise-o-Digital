

/*module adder_1bit(input wire a,
						input wire b,
						input wire cin, 
						output reg c,
						output reg cout);

	assign c = a ^ b ^ cin;

	assign cout = (a & b) | (a & cin) | (b & cin);

endmodule*/
